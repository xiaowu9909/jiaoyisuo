# 内容与语言规则（B 端 / C 端）

## 约定

1. **B 端（管理后台）**
   - 所有可编辑内容均以 **简体中文** 创建与维护。
   - **不提供** 英文输入项：C 端英文界面由系统自动翻译，无需在 B 端填写英文。

2. **C 端（用户前台）**
   - **中文界面**：请求配置时不带 `lang` 或 `lang≠en`，接口返回 B 端维护的中文原文。
   - **英文界面**：请求配置时带 `lang=en`，后端对配置中的中文文案自动翻译为英文后返回；C 端只展示接口返回的单一内容，无中/英字段分支。

## 实现要点

- **后端**：配置接口（如 `/api/config/home-getting-start`、`home-about-brand`、`home-app-download`）支持查询参数 `lang`；当 `lang=en` 时，在返回前对配置 JSON 中的中文字符串调用翻译服务（如 MyMemory）译为英文，再返回（图片 URL 等字段不翻译）。
- **B 端**：主页管理下各配置页仅保留简体中文字段（标题、副标题、描述、扫码/下载文案等），无 `*En` 字段。
- **C 端**：根据当前 `locale` 在请求配置时传入 `lang=locale=== 'en' ? 'en' : ''`；展示时统一使用接口返回的 `title`、`subtitle`、`detail` 等字段，无需再区分 `titleEn` / `title`。
