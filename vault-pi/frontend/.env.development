# 开发环境：使用相对路径 /api，请求会发到当前页同域（localhost:5174），由 Vite 代理到后端 8081，cookie 同域
# 请先启动后端（8081），再访问 http://localhost:5174
VITE_API_BASE=/api
VITE_BACKEND_PORT=8081
# 移动端「在线客服」快捷入口链接（可选），如不设则点击跳转帮助中心 /help
# VITE_SUPPORT_URL=https://your-support-chat.example.com
