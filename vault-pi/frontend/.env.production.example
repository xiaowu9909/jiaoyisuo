# 生产环境构建时使用。复制为 .env.production 并按实际域名修改。
# 若前端与后端同域（如 Nginx 反向代理 /api、/ws 到后端），可留空或设为 /api。
# 若前端部署在不同域名，需设为后端 API 根地址，例如：
# VITE_API_BASE=https://api.yourdomain.com/api
VITE_API_BASE=/api
# 可选：移动端「在线客服」链接
# VITE_SUPPORT_URL=https://your-support.example.com
